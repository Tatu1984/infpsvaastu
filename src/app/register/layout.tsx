export const metadata = {
  title: "Register | INFPSVaastu",
  description: "Create your INFPSVaastu account. Join as a property owner, agent, or builder to list and manage properties.",
}

export default function RegisterLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
