export const metadata = {
  title: "Search Properties | INFPSVaastu",
  description: "Search for properties to buy, rent, or lease. Filter by location, property type, budget, and more.",
}

export default function SearchLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
